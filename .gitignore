# Local continuation notes — keep on disk, never push
/PROGRESS.md

# Architecture diagrams stay on disk for local work; they are not in the GitHub repo.
/infra_v2.png
/infra_v2.svg
/infra_v2.txt
/infra_v3.png
/infra_v3.svg
/infra_v3.txt

# Local secrets — keep in this folder, never push
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local
secrets.env
**/secrets.env
*.pem

# Junction/symlink to the official skills submodule (recreate locally)
.claude/skills/cockroachdb-skills
