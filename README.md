# recall-agent
A persistent-memory AI research assistant that uses CockroachDB as its long-term memory layer and AWS for scalable agent execution. It stores, retrieves, and connects research knowledge, experimental results, decisions, tasks, and conversations through relational and vector-based memory, maintaining context and improve across sessions.
